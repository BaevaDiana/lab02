
#include <iostream> // подключение библиотеки
#include <string> // подключение библиотеки 
int
main()
{
  std::string name; / объявляем переменную для имени пользователя типа String
  std::cout << "Enter your name:"; //вывод в консоли
  std::cin >> name; //считываем имя из стандартного ввода
  std::cout << "Hello World from @" << name
            << "\n"; //выводим сообщение с именем пользователя с правильными проблеми
}
=======

#include <iostream> // подключение библиотеки
#include <string> // подключение библиотеки 

int

main()
{
std::string name; // объявляем переменную для имени пользователя типа String
std::cout << "Enter your name:"; //вывод в консоли
std::cin >> name; //считываем имя из стандартного ввода
std::cout << "Hello World from @" << name
         << "\n"; //выводим сообщение с именем пользователя с правильными проблеми
}
